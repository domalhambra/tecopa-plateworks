[pytest]
# Put the repo root on sys.path so `import app` / `import region_prep` resolve under a
# bare `pytest` invocation (CI), not just `python -m pytest` (which adds cwd itself).
pythonpath = .
testpaths = tests
