# Core runtime for the Tecopa Plateworks app + render engine.
# Exact pinned versions live in requirements-lock.txt (what CI installs); the ranges
# below are the human-readable minimums. Test stack: requirements-dev.txt. Offline
# region build (3DEP/NHD): requirements-regionprep.txt.
rasterio>=1.3
pyproj>=3.6
numpy>=1.26
scipy>=1.11
shapely>=2.0
gpxpy>=1.6
lxml>=5.0
Pillow>=10.2
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
